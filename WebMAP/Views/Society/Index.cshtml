@model IEnumerable<WebMAP.Models.SocietyVM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="m-portlet m-portlet--mobile">

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table table-striped- table-bordered table-hover table-checkable" id="m_table_1">
        <thead>
            <tr>
                <th>Description</th>
                <th>name</th>
                <th>startDate</th>
                <th>endDate</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in ViewBag.result)
            {
                <tr>
                    <td>
                        @item.description
                    </td>
                    <td>
                        @item.name
                    </td>
                    <td>
                        @item.startDate
                    </td>
                    <td>
                        @item.endDate
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.idSociety }) |
                        @Html.ActionLink("Details", "Details", new { id = item.idSociety }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.idSociety })
                    </td>
                </tr>
            }

        </tbody>
    </table>
</div>

